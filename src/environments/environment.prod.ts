/**
 * environment.prod.ts — Production environment configuration
 */
export const environment = {
  production: true,
  /** Base URL of the BluePatitas Spring Boot backend in production. */
  apiBaseUrl: 'https://api.bluepatitas.app',
};
